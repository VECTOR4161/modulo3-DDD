
generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "sqlite"
  url      = "file:./dev.db"
}

model Persona {
  id        Int    @id @default(autoincrement())
  nombres   String
  apellidos String
}
